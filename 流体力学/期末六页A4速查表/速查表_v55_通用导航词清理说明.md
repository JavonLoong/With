# v55 修正说明：通用导航词清理

本轮目标：继续把速查表从“旧题答案索引”转为“新卷模型定位表”。

已改动：

- `题库速解` 改为 `模型速解`。
- `带答案 / 压缩答案 / 到答案` 改为 `公式链 / 首行公式 / 流程`。
- `组合1、组合2...` 这类编号式标题删除编号，只保留模型组合名。
- 一处 `答案：` 改为 `公式链：`。

验收：

- PDF：`期末六页A4速查表_最终主用版_已修正_v55.pdf`
- 页数：6 页 A4。
- 编译日志：无 `Overfull`、无未定义命令、无缺字错误。
- 旧题导向词审计：`OldExamCueCount=0`。
- 预览：`preview_final_v55/page-1.png` 到 `page-6.png`。
