# 速查表 v48：查表动作与模型冲突补强说明

本版继续按“新卷通用”方向修正，不增加具体年份和题号。

## 修改内容

1. 将 M18 原先的元说明替换为“查教材表三行格式”：
   `查某表 -> 输入量 -> 输出量 -> 回代公式`。
2. 新增“模型冲突先判”：
   - 给 `mu, nu, epsilon, C_D`：优先粘性/阻力模型。
   - 给 `phi, psi, Gamma`：优先势流模型。
   - 给 `M, p0, T0, pb`：优先可压模型。
   - 给泵、阀、长管：优先机械能方程。
   - 给证明/说明：优先假设、方程、边界、结论。

## 验收记录

- PDF：`期末六页A4速查表_最终主用版_已修正_v48.pdf`
- 页数：6 页 A4。
- 日志关键词检查：无 `Overfull`、`Undefined control sequence`、`Missing character`、`LaTeX Error`、`Fatal error` 命中。
- 预览图：`preview_final_v48/page-1.png` 到 `page-6.png`。
