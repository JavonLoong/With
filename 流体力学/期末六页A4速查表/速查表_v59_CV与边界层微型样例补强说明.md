# v59 补强说明：控制体分量方程与边界层积分样例

本次继续执行“新卷通用定位”目标，不增加具体年份和题号。

依据 `速查表v16_全量测试评判报告` 的剩余风险，本次补强两个容易丢过程分的通用结构：

1. 复杂控制体方向：
   - 先画流入/流出；
   - 分量列动量方程；
   - 截面压力用表压且方向推入；
   - 题问管道、法兰、螺栓时取流体受力的反力。

2. 边界层动量积分：
   - 设速度型 `u/U=f(eta)`；
   - 先算 `delta*`、`theta`、`tau_w`；
   - 零压梯度用 `tau_w=rho U^2 dtheta/dx`；
   - 求出 `delta(x)` 后再收口阻力系数。

落点：

- M26：高频计算/查表/控制体收口。
- M17：边界层积分长题仍保留更完整推导链。
- Q2：控制体题仍保留通用受力模板。

验收：

- PDF：`期末六页A4速查表_最终主用版_已修正_v59.pdf`
- 页数：6 页 A4。
- 日志：无 `Overfull`、无 `Undefined control sequence`、无 `LaTeX Error`。
- 预览：`preview_final_v59/page-1.png` 到 `page-6.png`。
