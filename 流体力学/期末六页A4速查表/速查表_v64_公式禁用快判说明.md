# 速查表 v64 公式禁用快判说明

本次修改目标：继续服务“换新卷也能按题干定位模型”，尤其处理陌生题中最常见的错误：公式套用条件不成立。

## 新增内容

新增 `M29 公式禁用快判`，覆盖：

- Bernoulli 何时不能直接用；
- H--P 何时不能直接用；
- 等熵关系何时不能直接用；
- `0.528` 临界压比何时不能直接用；
- Moody 图何时不能用，何时直接用 `lambda=64/Re`；
- 势流压力阻力结论何时不能用于真实阻力题。

## 验证结果

- PDF：`期末六页A4速查表_最终主用版_已修正_v64.pdf`
- 页数：6 页，A4。
- 日志检查：无 `Overfull`、无 `Missing character`、无未定义命令、无 LaTeX fatal error。
- 旧题导向词计数：`往年=0`，`年份=0`，`题号=0`，`原题=0`，`旧题=0`，`押题=0`。
- 预览图：`preview_final_v64/page-1.png` 到 `page-6.png`。

